import React, { Component } from 'react';

// eslint-disable-next-line no-unused-vars
import NavigationRouter from '../components/navigationRouter.component';

export default class Header extends Component {
    render() {
        return(
            <NavigationRouter />
        )
    }
}